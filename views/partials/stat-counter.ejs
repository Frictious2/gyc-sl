<div class="stat-card h-100">
  <div class="stat-value"><%= stat.value %></div>
  <div class="stat-label"><%= stat.label %></div>
  <% if (stat.description) { %>
    <p class="small text-muted mb-0 mt-2"><%= stat.description %></p>
  <% } %>
</div>
